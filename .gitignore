# Keys (sollten nicht committed werden)
keys/
*.pem
*.key
!public-keys/

# Environment variables
.env
.env.local
.env.*.local

# Demo presigned URLs (niemals committen — enthält temporäre S3-Signaturen)
config/demo-presigned-fallback.json
config/tempus-demo-pm-media-slots.json

# Local credentials (never commit)
.credentials/
*.local.json

# Node modules
node_modules/

# Build outputs
dist/
build/
onboarding/
*.log

# Deploy-Cache (Smart-Build-Detection)
.deploy-cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp/
temp/
*.tmp

# CDK Output
infrastructure/cdk.out/
infrastructure/cdk.out/**/*
lambda/pdf-generator-gpt52.zip

# Lambda deployment ZIP files
lambda/**/*.zip
*.zip

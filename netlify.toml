# Netlify Configuration
# Optimiert für Credit-Sparsamkeit

[build]
  # Kein Build-Command nötig, da statische Seite
  command = ""
  publish = "."

# Branch Deploy Settings - NUR Production Branch deployt
[context.production]
  command = ""
  publish = "."

# Preview Deploys (Pull Requests) - DEAKTIVIERT um Credits zu sparen
[context.deploy-preview]
  command = ""
  publish = "."
  # Deaktiviert - keine Preview Deploys
  skip_processing = true

# Branch Deploys - DEAKTIVIERT um Credits zu sparen
[context.branch-deploy]
  command = ""
  publish = "."
  # Deaktiviert - keine Branch Deploys
  skip_processing = true

# Production Branch - NUR dieser Branch deployt
[build.processing]
  skip_processing = false

# Headers für Performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

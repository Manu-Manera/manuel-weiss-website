# Lokale AWS-Nutzung (Hero-Video, Deploy-Skripte)
# Credentials NUR via: aws configure --profile <name>
# Nicht committen. Nicht in Cursor-Chat posten.

AWS_REGION=eu-central-1
# Optional: AWS_PROFILE=manuel-website-deploy

AWS_S3_HERO_VIDEO_BUCKET=manuel-weiss-hero-videos
DYNAMODB_SETTINGS_TABLE=manuel-weiss-settings
